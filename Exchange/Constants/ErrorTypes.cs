namespace Exchange.Constants
{
    public enum ErrorTypes
    {
        UserNotFound,
        InvalidPassword,
        DeviceAuthRemoved,
        InvalidToken,
        ExpiredToken,
        InvalidParameters
    }
}
